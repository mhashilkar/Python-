"""
    1.Required arguments are the arguments passed to the function in correct order
    2.Here,the number of arguments in the function call should match exactly with the function definition
"""
def printme(str):
    print(str)
    return

printme()

# this code will give error becz we have not given Required Argument
